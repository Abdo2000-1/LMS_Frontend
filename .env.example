# Local development — copy to .env and adjust as needed

# .NET LMS Backend API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:5102

# Optional: Vite public base path (default for GitHub Pages subpath)
# VITE_BASE_PATH=/LMS_Frontend/
